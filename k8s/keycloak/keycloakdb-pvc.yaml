apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kcdb-pvc
  namespace: keycloak
  labels:
    pvc: db
spec:
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 100Mi
  accessModes:
    - ReadWriteOnce